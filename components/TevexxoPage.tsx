'use client';

import Link from 'next/link';
import { FormEvent, useEffect, useMemo, useState } from 'react';
import { ArrowRight, Check, ChevronLeft, ChevronRight, CirclePlay, Cloud, Code2, Database, Globe2, Menu, Search, ShieldCheck, Sparkles, Star, X, Zap } from 'lucide-react';
import { Logo } from './Logo';
import { courses, navItems, projects, testimonials, type Course, type Project } from '@/lib/tevexxo-data';
import Image from 'next/image';

const iconMap = { 'AI & Machine Learning': Sparkles, 'Data Analytics': Database, 'Full Stack Development': Code2, 'Cyber Security': ShieldCheck, 'Cloud & DevOps': Cloud, 'UI/UX Design': Zap };

function Header({ onSearch, onDemo }: { onSearch: () => void; onDemo: () => void }) {
  const [open, setOpen] = useState(false);
  useEffect(() => { document.body.style.overflow = open ? 'hidden' : ''; return () => { document.body.style.overflow = ''; }; }, [open]);
  return <header className="absolute left-0 right-0 top-0 z-40 border-b border-white/10 bg-[#07121e]/70 text-white backdrop-blur-md">
    <div className="container-shell flex h-[76px] items-center justify-between gap-6">
      <Link href="/" aria-label="Tevexxo home"><Logo compact /></Link>
      <nav className="hidden items-center gap-7 lg:flex" aria-label="Primary navigation">{navItems.map(item => <Link key={item.href} href={item.href} className="text-sm text-white/70 transition hover:text-white">{item.label}</Link>)}</nav>
      <div className="hidden items-center gap-3 sm:flex"><button onClick={onSearch} aria-label="Search" className="rounded-full p-2 text-white/70 transition hover:bg-white/10 hover:text-white"><Search size={19} /></button><button onClick={onDemo} className="rounded-full bg-[#ff5a00] px-5 py-3 text-sm font-semibold text-white transition hover:bg-[#ff6e21]">Get started</button></div>
      <button onClick={() => setOpen(true)} aria-label="Open menu" className="rounded-lg p-2 text-white lg:hidden"><Menu /></button>
    </div>
    {open && <div className="fixed inset-0 bg-[#07121e] p-5 lg:hidden"><div className="flex items-center justify-between"><Link href="/" onClick={() => setOpen(false)}><Logo compact /></Link><button onClick={() => setOpen(false)} aria-label="Close menu"><X /></button></div><nav className="mt-16 grid gap-5 text-2xl font-semibold">{navItems.map(item => <Link key={item.href} href={item.href} onClick={() => setOpen(false)}>{item.label}</Link>)}<button onClick={() => { setOpen(false); onDemo(); }} className="mt-4 w-fit rounded-full bg-[#ff5a00] px-6 py-3 text-base">Book a free demo</button></nav></div>}
  </header>;
}

function SearchModal({ onClose }: { onClose: () => void }) {
  const [query, setQuery] = useState('');
  const results = useMemo(() => [...courses.map(course => ({ ...course, kind: 'Course' })), ...projects.map(project => ({ ...project, kind: 'Project' }))].filter(item => item.title.toLowerCase().includes(query.toLowerCase())), [query]);
  useEffect(() => { const onKey = (event: KeyboardEvent) => event.key === 'Escape' && onClose(); window.addEventListener('keydown', onKey); return () => window.removeEventListener('keydown', onKey); }, [onClose]);
  return <div className="fixed inset-0 z-50 flex items-start justify-center bg-[#07121e]/80 p-4 pt-[12vh] backdrop-blur-sm" onMouseDown={event => event.currentTarget === event.target && onClose()}><div className="w-full max-w-2xl overflow-hidden rounded-3xl bg-white shadow-2xl"><div className="flex items-center gap-3 border-b border-slate-200 p-5"><Search className="text-[#ff5a00]" size={22} /><input autoFocus value={query} onChange={event => setQuery(event.target.value)} placeholder="Search courses and projects" className="w-full text-lg outline-none" /><button onClick={onClose} aria-label="Close search"><X /></button></div><div className="max-h-[52vh] overflow-y-auto p-3">{results.length ? results.map(item => <Link onClick={onClose} href={'slug' in item && item.kind === 'Project' ? `/projects/${item.slug}` : '/courses'} key={item.title} className="flex items-center justify-between rounded-2xl p-4 transition hover:bg-orange-50"><div><p className="font-semibold">{item.title}</p><p className="mt-1 text-sm text-slate-500">{item.kind}</p></div><ArrowRight size={18} className="text-[#ff5a00]" /></Link>) : <p className="p-8 text-center text-slate-500">No matches yet. Try another keyword.</p>}</div></div></div>;
}

function DemoModal({ onClose }: { onClose: () => void }) {
  const [submitted, setSubmitted] = useState(false); const [busy, setBusy] = useState(false); const [error, setError] = useState('');
  const submit = (event: FormEvent<HTMLFormElement>) => { event.preventDefault(); setError(''); const form = new FormData(event.currentTarget); const email = String(form.get('email') || ''); if (!String(form.get('name') || '').trim() || !email.includes('@') || !String(form.get('phone') || '').trim() || !form.get('date')) { setError('Please complete the required fields with valid details.'); return; } setBusy(true); window.setTimeout(() => { setBusy(false); setSubmitted(true); }, 700); };
  return <div className="fixed inset-0 z-50 flex items-center justify-center bg-[#07121e]/80 p-4 backdrop-blur-sm" onMouseDown={event => event.currentTarget === event.target && onClose()}><div className="w-full max-w-xl rounded-3xl bg-white p-6 shadow-2xl sm:p-8"><div className="flex items-start justify-between"><div><p className="text-sm font-semibold uppercase tracking-[0.2em] text-[#ff5a00]">Start with clarity</p><h2 className="mt-2 font-display text-3xl font-bold">Book your free demo</h2></div><button onClick={onClose} aria-label="Close form"><X /></button></div>{submitted ? <div className="py-14 text-center"><div className="mx-auto flex h-14 w-14 items-center justify-center rounded-full bg-green-100 text-green-700"><Check /></div><h3 className="mt-5 text-2xl font-bold">You&apos;re on the list.</h3><p className="mt-2 text-slate-500">Our learning advisor will reach out with the next steps.</p><button onClick={onClose} className="mt-7 rounded-full bg-[#ff5a00] px-6 py-3 font-semibold text-white">Close</button></div> : <form onSubmit={submit} className="mt-7 grid gap-4 sm:grid-cols-2"><label className="grid gap-2 text-sm font-medium">Name *<input name="name" className="rounded-xl border border-slate-200 px-4 py-3 outline-none focus:border-[#ff5a00]" placeholder="Your name" /></label><label className="grid gap-2 text-sm font-medium">Email *<input name="email" type="email" className="rounded-xl border border-slate-200 px-4 py-3 outline-none focus:border-[#ff5a00]" placeholder="you@example.com" /></label><label className="grid gap-2 text-sm font-medium">Phone *<input name="phone" className="rounded-xl border border-slate-200 px-4 py-3 outline-none focus:border-[#ff5a00]" placeholder="+91 00000 00000" /></label><label className="grid gap-2 text-sm font-medium">Preferred date *<input name="date" type="date" className="rounded-xl border border-slate-200 px-4 py-3 outline-none focus:border-[#ff5a00]" /></label><label className="grid gap-2 text-sm font-medium sm:col-span-2">Interested course<select name="course" className="rounded-xl border border-slate-200 bg-white px-4 py-3"><option>Full Stack Development</option>{courses.filter(course => course.title !== 'Full Stack Development').map(course => <option key={course.slug}>{course.title}</option>)}</select></label><label className="grid gap-2 text-sm font-medium sm:col-span-2">Message<textarea name="message" rows={3} className="rounded-xl border border-slate-200 px-4 py-3 outline-none focus:border-[#ff5a00]" placeholder="What are you hoping to build?" /></label>{error && <p className="text-sm text-red-600 sm:col-span-2">{error}</p>}<button disabled={busy} className="rounded-xl bg-[#ff5a00] px-5 py-3 font-semibold text-white transition hover:bg-[#e94f00] disabled:opacity-60 sm:col-span-2">{busy ? 'Sending...' : 'Request my free demo'}</button></form>}</div></div>;
}

function CourseCard({ course }: { course: Course }) { const Icon = iconMap[course.title as keyof typeof iconMap] || Globe2; return <Link href="/courses" className="group rounded-3xl border border-slate-200 bg-white p-6 shadow-sm transition-all duration-300 ease-out hover:-translate-y-1 hover:border-[#ff5a00] hover:shadow-xl"><div className="flex items-start justify-between"><div className="flex h-12 w-12 items-center justify-center rounded-2xl bg-orange-50 text-[#ff5a00]"><Icon size={24} /></div><ArrowRight size={20} className="text-slate-300 transition group-hover:translate-x-1 group-hover:text-[#ff5a00]" /></div><h3 className="mt-6 font-display text-xl font-bold">{course.title}</h3><p className="mt-3 min-h-[52px] text-sm leading-6 text-slate-500">{course.description}</p><div className="mt-5 flex flex-wrap gap-2">{course.tags.map(tag => <span key={tag} className="rounded-full bg-slate-100 px-3 py-1 text-xs text-slate-600">{tag}</span>)}</div><div className="mt-6 flex items-center justify-between border-t border-slate-100 pt-4 text-xs text-slate-500"><span>{course.duration} · {course.level}</span><span className="flex items-center gap-1 font-semibold text-slate-700"><Star size={13} fill="#ff5a00" className="text-[#ff5a00]" /> {course.rating} ({course.reviews})</span></div></Link>; }

function ProjectVisual({ project }: { project: Project }) { return <div className="relative h-56 overflow-hidden rounded-2xl bg-slate-100"><Image src={project.image} alt={project.title} fill sizes="(max-width: 1024px) 100vw, 360px" className="object-cover" /></div>; }

function ProjectCard({ project }: { project: Project }) { return <article className="rounded-3xl border border-slate-200 bg-white p-4 shadow-sm transition hover:-translate-y-1 hover:shadow-xl"><ProjectVisual project={project} /><div className="p-3"><div className="flex items-center justify-between gap-3"><span className="text-xs font-semibold uppercase tracking-[0.15em] text-[#ff5a00]">{project.type}</span>{project.featured && <span className="rounded-full bg-orange-100 px-3 py-1 text-xs font-semibold text-[#d84700]">Featured</span>}</div><h3 className="mt-3 font-display text-xl font-bold">{project.title}</h3><p className="mt-2 text-sm leading-6 text-slate-500">{project.description}</p><div className="mt-4 flex flex-wrap gap-2">{project.tags.map(tag => <span key={tag} className="rounded-full bg-slate-100 px-3 py-1 text-xs text-slate-600">{tag}</span>)}</div><div className="mt-5 flex items-center justify-between"><Link href={`/projects/${project.slug}`} className="flex items-center gap-2 text-sm font-semibold text-[#ff5a00]">View case study <ArrowRight size={16} /></Link><span className="text-xs text-slate-400">{project.views} views</span></div></div></article>; }

function Footer() { return <footer className="bg-[#07121e] pb-8 pt-16 text-white"><div className="container-shell grid gap-12 md:grid-cols-[1.5fr_repeat(3,1fr)]"><div><Logo compact /><p className="mt-5 max-w-xs text-sm leading-6 text-white/55">Practical technology education for people building what comes next.</p><div className="mt-6 flex gap-3"><a href="https://www.linkedin.com" target="_blank" rel="noreferrer" className="rounded-full border border-white/15 px-3 py-2 text-xs text-white/70 hover:border-[#ff5a00] hover:text-white">LinkedIn</a><a href="https://www.instagram.com" target="_blank" rel="noreferrer" className="rounded-full border border-white/15 px-3 py-2 text-xs text-white/70 hover:border-[#ff5a00] hover:text-white">Instagram</a></div></div>{[['Learn', [['Courses', '/courses'], ['Programs', '/programs'], ['Projects', '/projects']]], ['Company', [['About us', '/about'], ['Contact', '/contact']]], ['Support', [['Book a demo', '/contact'], ['FAQ', '/contact']]]].map(([heading, links]) => <div key={heading as string}><h3 className="font-semibold">{heading as string}</h3><div className="mt-5 grid gap-3 text-sm text-white/55">{(links as string[][]).map(([label, href]) => <Link key={href} href={href} className="transition hover:text-white">{label}</Link>)}</div></div>)}</div><div className="container-shell mt-14 flex flex-col justify-between gap-3 border-t border-white/10 pt-6 text-xs text-white/35 sm:flex-row"><span>© 2026 Tevexxo Learning Private Limited.</span><span>Made for curious minds.</span></div></footer>; }

export default function TevexxoPage() {
  const [searchOpen, setSearchOpen] = useState(false); const [demoOpen, setDemoOpen] = useState(false); const [testimonial, setTestimonial] = useState(0); const [email, setEmail] = useState(''); const [newsletter, setNewsletter] = useState('idle');
  const submitNewsletter = (event: FormEvent<HTMLFormElement>) => { event.preventDefault(); if (!email.includes('@')) { setNewsletter('error'); return; } setNewsletter('success'); setEmail(''); };
  return <div><section className="relative overflow-hidden bg-[#07121e] text-white"><Header onSearch={() => setSearchOpen(true)} onDemo={() => setDemoOpen(true)} /><div className="container-shell relative grid min-h-[720px] items-center gap-12 pb-20 pt-36 lg:grid-cols-[1.02fr_0.98fr] lg:pb-24"><div className="relative z-10"><div className="mb-6 inline-flex items-center gap-2 rounded-full border border-white/15 bg-white/5 px-4 py-2 text-xs text-white/70"><span className="h-2 w-2 rounded-full bg-[#ff5a00]" /> The future belongs to the builders</div><h1 className="max-w-2xl font-display text-5xl font-bold leading-[1.03] tracking-[-0.06em] sm:text-6xl lg:text-7xl">Learn skills.<br /><span className="text-[#ff5a00]">Build boldly.</span></h1><p className="mt-7 max-w-lg text-base leading-7 text-white/60 sm:text-lg">Career-focused learning, expert mentorship, and real-world projects that take you from curious to capable.</p><div className="mt-9 flex flex-col gap-3 sm:flex-row"><Link href="/courses" className="inline-flex items-center justify-center gap-2 rounded-full bg-[#ff5a00] px-6 py-3.5 font-semibold transition hover:bg-[#ff6e21]">Explore courses <ArrowRight size={18} /></Link><button onClick={() => setDemoOpen(true)} className="inline-flex items-center justify-center gap-2 rounded-full border border-white/20 px-6 py-3.5 font-semibold text-white transition hover:border-white/50"><CirclePlay size={18} /> Book a free demo</button></div><div className="mt-12 flex items-center gap-4"><div className="flex -space-x-3">{['AM', 'MS', 'RK', 'PS'].map(initials => <div key={initials} className="flex h-10 w-10 items-center justify-center rounded-full border-2 border-[#07121e] bg-gradient-to-br from-orange-300 to-orange-600 text-xs font-bold">{initials}</div>)}</div><div><p className="text-sm font-semibold">10,000+ active learners</p><p className="text-xs text-white/45">Learning with Tevexxo</p></div></div></div><div className="relative mx-auto w-full max-w-[520px] lg:ml-auto"><div className="absolute -inset-16 rounded-full bg-orange-500/10 blur-3xl" /><div className="relative rounded-[40px] border border-white/10 bg-white/[0.06] p-4 shadow-2xl shadow-black/30"><div className="relative overflow-hidden rounded-[30px] bg-[#102d3b] p-8 pt-12"><div className="absolute right-7 top-7 flex gap-2"><span className="h-2 w-2 rounded-full bg-[#ff5a00]" /><span className="h-2 w-2 rounded-full bg-white/30" /></div><div className="mx-auto flex h-56 w-56 items-center justify-center rounded-full border border-orange-300/20 bg-orange-400/10 shadow-[0_0_90px_rgba(255,90,0,.24)]"><div className="h-36 w-36 rounded-full border border-orange-300/40 bg-gradient-to-br from-orange-300 via-[#ff5a00] to-[#6b2200] shadow-[inset_-20px_-25px_40px_rgba(0,0,0,.35)]" /></div><div className="mt-10 grid grid-cols-3 gap-3"><div className="rounded-2xl bg-white/10 p-4"><p className="text-xs text-white/50">Learning hours</p><p className="mt-2 font-display text-2xl font-bold">1.2M</p></div><div className="rounded-2xl bg-white/10 p-4"><p className="text-xs text-white/50">Projects built</p><p className="mt-2 font-display text-2xl font-bold">4.8K</p></div><div className="rounded-2xl bg-[#ff5a00] p-4"><p className="text-xs text-white/70">Job ready</p><p className="mt-2 font-display text-2xl font-bold">95%</p></div></div></div></div><div className="absolute -left-5 top-28 rounded-2xl border border-white/15 bg-[#0b1c2a]/90 px-4 py-3 shadow-xl backdrop-blur"><p className="text-[10px] uppercase tracking-widest text-white/45">Popular path</p><p className="mt-1 text-sm font-semibold">Full Stack Development</p></div><div className="absolute -bottom-4 -right-3 rounded-2xl border border-white/15 bg-[#0b1c2a]/90 px-4 py-3 shadow-xl backdrop-blur"><p className="text-[10px] uppercase tracking-widest text-white/45">Next milestone</p><p className="mt-1 text-sm font-semibold text-[#ff8b52]">Ship your first product</p></div></div></div><div className="absolute -left-24 top-20 h-56 w-56 rounded-full border border-white/5" /><div className="absolute -right-32 bottom-0 h-80 w-80 rounded-full border border-orange-500/10" /></section>
    <section className="border-b border-slate-200 bg-white py-12"><div className="container-shell grid grid-cols-2 gap-8 md:grid-cols-4"><div><p className="font-display text-4xl font-bold">10K<span className="text-[#ff5a00]">+</span></p><p className="mt-2 text-sm text-slate-500">Active learners</p></div><div><p className="font-display text-4xl font-bold">25<span className="text-[#ff5a00]">+</span></p><p className="mt-2 text-sm text-slate-500">Industry programs</p></div><div><p className="font-display text-4xl font-bold">95<span className="text-[#ff5a00]">%</span></p><p className="mt-2 text-sm text-slate-500">Completion rate</p></div><div><p className="font-display text-4xl font-bold">50<span className="text-[#ff5a00]">+</span></p><p className="mt-2 text-sm text-slate-500">Expert mentors</p></div></div></section>
    <section className="container-shell py-24"><div className="flex flex-col justify-between gap-6 md:flex-row md:items-end"><div><p className="text-sm font-semibold uppercase tracking-[0.2em] text-[#ff5a00]">Choose your edge</p><h2 className="mt-3 max-w-xl font-display text-4xl font-bold tracking-[-0.04em] sm:text-5xl">A better way to learn what&apos;s next.</h2></div><Link href="/courses" className="flex items-center gap-2 text-sm font-semibold text-[#ff5a00]">View all courses <ArrowRight size={18} /></Link></div><div className="mt-12 grid gap-5 md:grid-cols-2 lg:grid-cols-3">{courses.map(course => <CourseCard key={course.slug} course={course} />)}</div></section>
    <section className="bg-[#eef2f1] py-24"><div className="container-shell"><div className="max-w-2xl"><p className="text-sm font-semibold uppercase tracking-[0.2em] text-[#ff5a00]">Proof over promises</p><h2 className="mt-3 font-display text-4xl font-bold tracking-[-0.04em] sm:text-5xl">Build a portfolio that opens doors.</h2><p className="mt-5 text-slate-500">Every Tevexxo journey ends with work you can explain, share, and be proud of.</p></div><div className="mt-12 grid gap-6 lg:grid-cols-3">{projects.map(project => <ProjectCard key={project.slug} project={project} />)}</div></div></section>
    <section className="container-shell py-24"><div className="grid gap-12 lg:grid-cols-[0.8fr_1.2fr] lg:items-center"><div><p className="text-sm font-semibold uppercase tracking-[0.2em] text-[#ff5a00]">Learner stories</p><h2 className="mt-3 font-display text-4xl font-bold tracking-[-0.04em] sm:text-5xl">Momentum feels better together.</h2><div className="mt-8 flex gap-3"><button onClick={() => setTestimonial((testimonial - 1 + testimonials.length) % testimonials.length)} aria-label="Previous testimonial" className="rounded-full border border-slate-200 p-3 transition hover:border-[#ff5a00] hover:text-[#ff5a00]"><ChevronLeft size={20} /></button><button onClick={() => setTestimonial((testimonial + 1) % testimonials.length)} aria-label="Next testimonial" className="rounded-full border border-slate-200 p-3 transition hover:border-[#ff5a00] hover:text-[#ff5a00]"><ChevronRight size={20} /></button></div></div><div className="rounded-3xl bg-[#07121e] p-8 text-white sm:p-12"><div className="flex gap-1 text-[#ff5a00]">{Array.from({ length: 5 }).map((_, index) => <Star key={index} size={17} fill="currentColor" />)}</div><p className="mt-7 max-w-2xl font-display text-2xl font-semibold leading-snug sm:text-3xl">&quot;{testimonials[testimonial].quote}&quot;</p><div className="mt-10 flex items-center gap-4"><div className="flex h-11 w-11 items-center justify-center rounded-full bg-[#ff5a00] text-sm font-bold">{testimonials[testimonial].initials}</div><div><p className="font-semibold">{testimonials[testimonial].name}</p><p className="mt-1 text-sm text-white/50">{testimonials[testimonial].role}</p></div></div></div></div></section>
    <section className="bg-[#ff5a00] py-20"><div className="container-shell grid gap-8 text-white md:grid-cols-[1fr_0.8fr] md:items-center"><div><h2 className="max-w-xl font-display text-4xl font-bold tracking-[-0.04em] sm:text-5xl">Ready to upgrade your tech skills?</h2><p className="mt-4 max-w-lg text-white/75">Get one useful idea, project prompt, and career note in your inbox each week.</p></div><form onSubmit={submitNewsletter} className="flex flex-col gap-3 sm:flex-row md:flex-col lg:flex-row"><input value={email} onChange={event => { setEmail(event.target.value); setNewsletter('idle'); }} type="email" placeholder="Your email address" className="min-w-0 flex-1 rounded-full border-0 bg-white px-5 py-3.5 text-sm text-slate-900 outline-none" aria-label="Email address" /><button className="rounded-full bg-[#07121e] px-6 py-3.5 text-sm font-semibold text-white transition hover:bg-[#12283a]">Start learning</button>{newsletter === 'success' && <span className="text-sm font-medium">You&apos;re subscribed.</span>}{newsletter === 'error' && <span className="text-sm font-medium">Enter a valid email.</span>}</form></div></section><Footer />{searchOpen && <SearchModal onClose={() => setSearchOpen(false)} />}{demoOpen && <DemoModal onClose={() => setDemoOpen(false)} />}</div>;
}
